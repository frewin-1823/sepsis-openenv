[project]
name = "sepsis-openenv"
version = "1.0.0"
description = "Sepsis ICU OpenEnv Environment"
requires-python = ">=3.10"

[tool.openenv]
environment = "server.app:app"

[project.scripts]
server = "server.app:app"